{
  "framework": "vite",
  "buildCommand": "cd .. && pnpm run build --filter=@the-toast/shared --filter=@the-toast/client",
  "outputDirectory": "dist",
  "installCommand": "cd .. && pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
